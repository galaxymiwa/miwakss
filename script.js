const canvas=document.getElementById('stars'),ctx=canvas.getContext('2d');let W,H,stars=[];function resize(){W=canvas.width=innerWidth*devicePixelRatio;H=canvas.height=innerHeight*devicePixelRatio;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';stars=Array.from({length:Math.min(420,Math.floor(innerWidth/2))},()=>({x:Math.random()*W,y:Math.random()*H,r:Math.random()*1.7+.2,a:Math.random()*.8+.15,s:Math.random()*.008+.002}))}resize();addEventListener('resize',resize);function draw(){ctx.clearRect(0,0,W,H);for(const s of stars){s.a+=Math.sin(Date.now()*s.s)*.002;ctx.fillStyle=`rgba(255,244,214,${Math.max(.05,Math.min(.9,s.a))})`;ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fill()}requestAnimationFrame(draw)}draw();
function flower(x,y,scale=1){const d=document.createElement('div');d.className='flower';d.style.left=x+'%';d.style.bottom=y+'%';d.style.transform=`scale(${scale})`;d.style.animationDelay=(Math.random()*-5)+'s';d.innerHTML='<div class="stem"></div><div class="leaf"></div><div class="leaf r"></div><div class="bloom">'+Array.from({length:8},(_,i)=>`<i class="petal p${i+1}"></i>`).join('')+'<i class="center"></i></div>';return d}const field=document.getElementById('field');for(let i=0;i<44;i++){const f=flower(Math.random()*100,-2+Math.random()*3,.55+Math.random()*1.1);field.appendChild(f)}const ff=document.getElementById('finalFlowers');for(let i=0;i<16;i++)ff.appendChild(flower(Math.random()*100,-2,.5+Math.random()*.9));
const petals=document.getElementById('petals');function petal(){const p=document.createElement('span');p.style.position='fixed';p.style.left=Math.random()*100+'vw';p.style.top='-20px';p.style.width=5+Math.random()*8+'px';p.style.height=10+Math.random()*15+'px';p.style.background='rgba(255,214,77,.8)';p.style.borderRadius='80% 20% 80% 20%';p.style.zIndex=5;p.style.pointerEvents='none';const dur=6+Math.random()*8;p.animate([{transform:'translate(0,0) rotate(0deg)'},{transform:`translate(${(Math.random()-.5)*35}vw,110vh) rotate(${Math.random()*900-450}deg)`}],{duration:dur*1000,easing:'linear'}).onfinish=()=>p.remove();petals.appendChild(p)}setInterval(petal,650);
document.getElementById('begin').onclick=()=>document.querySelector('.quote').scrollIntoView({behavior:'smooth'});document.getElementById('replay').onclick=()=>scrollTo({top:0,behavior:'smooth'});
